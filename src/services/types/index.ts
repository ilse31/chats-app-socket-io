export type registerData = {
  username: string;
  password: string;
  email: string;
};

export type loginData = {
  email: string;
  password: string;
};

export type getMeData = {
  userId: string;
};
